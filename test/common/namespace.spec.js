
describe('---------- emphasize namespace ----------', () => {

	'use strict';

	it('should be defined', () => {
		expect(emphasize).toBeDefined();
	});

	it('should have a `pool` property', () => {
		expect(emphasize.pool).toBeDefined();
	});

	it('should have a `app` property', () => {
		expect(emphasize.pool).toBeDefined();
	});
});